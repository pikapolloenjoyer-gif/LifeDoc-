import tkinter as tk

def saludar():
    nombre = entrada.get()
    etiqueta.config(text=f"Hola {nombre}")

ventana = tk.Tk()
ventana.title("Diario")

entrada = tk.Entry(ventana)
entrada.pack()

boton = tk.Button(ventana, text="Highlight del día")
boton.pack()

etiqueta = tk.Label(ventana, text="")
etiqueta.pack()

ventana.mainloop()